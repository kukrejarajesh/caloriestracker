targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            calorie_tracker: lib/data/database/app_database.dart
          schema_dir: drift_schemas/
          test_dir: test/data/database/
